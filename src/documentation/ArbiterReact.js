import { draw } from "./draw"

export const ArbiterReact = draw(({ a, any, warning, either, optional }) => (
    a.package({ title: 'Arbiter-React', name: '@triframe/arbiter-react' },{
        
    })
))